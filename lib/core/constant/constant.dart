enum Lang {
  AR,
  EN,
}

enum ThemeType{
  light,
  dark,
}

enum UserType {
  ORG,
  BU,
  EMP,
}

const HTTP_API_URL = 'https://dona-soft.com/api/';

const HTTP_COURSES = '';
const HTTP_CVERSION = '';
const HTTP_PROFILE = '';
const HTTP_USER_PROFILE = '';
const HTTP_ORG_PROFILE = '';
