import 'package:flutter/material.dart';

class BoardPanal extends StatelessWidget {
  const BoardPanal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
